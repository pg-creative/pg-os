"""Personal OS integrations — CLI mirrors of OAuth services.

The existing Next.js app (src/lib/*.ts) handles OAuth + live UI. These scripts
write markdown summaries that the snapshot skill + dashboard read.
"""
