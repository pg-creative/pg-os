# Canon: the OS front door (prompt, 2026-09-02)

Run at effort: xhigh. Read this whole file first. Decisions below are already made with PG in a grill session on 2026-09-02; do not reopen them.

## Decisions already locked

- **Words (locked 2026-09-02):** the OS is the **World**; the identity layer inside it is the **Canon**; the story the Canon tells is the **Myth**; the reference wall is the **Lore**. "Mythos" was the working word and is retired. Use these four everywhere, in UI and in files.
- **Subject:** in PG's words, "this is about me ultimately and my life and my brain and my genius and my expression and my dreams and aesthetic." Maygan and everyone else are pieces in it, party members, not the center. The Canon's regions are his six words: Life, Brain, Genius, Expression, Dreams, Aesthetic.
- **Shape:** the Canon is the identity layer of Personal OS, built inside this repo (`~/cortex/personal-os`) on the Emaki x Laputa canon. Not a new repo, not a new app, not a doc in `self/`. The command space with agent modules already exists here (Bridge, Capture FAB, cmd-J copilot, Kitsu); the mythos is what has never existed: the answer to "who is the player."
- **What it is, in PG's words:** "a who I am and who I am becoming: a self narrative and mood board and expressive space." Myth text, signs, images, quotes, chapters. One space.
- **Devotion, in PG's words:** "Devotion, for me, is showing up for the version of me I'm becoming, the way I'd show up for someone I love." This is the organizing frame. Discipline is the stick, devotion is the carrot; positive over negative.
- **Input:** evening pages, typed, fast, at night, on laptop some nights and phone others. First one is at `~/cortex/knowledge/journal/pages/2026-09-01.md`. Read it before anything else; it is the freshest evidence and the voice the mythos is written in.
- **Witness:** PG only. An agent reads the pages back to him on a cadence, updates the myth, and tells him what changed. No share link, no public surface.

## Read first, in this order

1. `CLAUDE.md` here (the OS is LIVE at pg-os.vercel.app; PWA on his iPhone; do not treat it as a rebuild)
2. `design-system/CANON.md` and `src/app/globals.css` variants (laputa-day / twilight / midnight; light by day, never permanent dark)
3. `src/app/_components/views/BridgeView.tsx`, `HomeView.tsx`, `PersonalView.tsx`, `src/lib/capture.ts` (destinations incl. `hc-journal`), `src/app/api/journal/*`, `supabase/migrations/`
4. `~/cortex/knowledge/journal/pages/2026-09-01.md`
5. `~/cortex/self/LEDGER.md` (never-restart rule), `self/center-of-self.html` (Entries 001-003), `self/.claude/memory/center-of-self.md`, `self/brand/BRAND.md`
6. `~/cortex/claude-web-history/docs/extraction-2026-07-13-master-findings.md`, `cognitive-audit.md`, `probe-results.md`

Then say back in plain words what Entry 001 concluded about discipline (relational; the gap was never discipline; fresh containers get rebuilt at the moment they would hold him because a fresh one offers innocence) and confirm you understand the mythos is Entry 004 continuing that, and that this build must not become container number five. Everything here extends the live OS.

## Phase 0: /grill-me on what is still open

Run `/grill-me`. Only these are undecided; one question at a time, batched options plus a recommendation, never a bare menu:
- Where evening pages get written on each device. Recommendation to put to him: the OS itself on both (laptop browser, iPhone PWA), a new capture destination `pages` alongside `hc-journal`, stored in the HC Supabase journal table, and a nightly export to `~/cortex/knowledge/journal/pages/YYYY-MM-DD.md` so Claude Code and the archive always have plain files. This makes Notion unnecessary; he said "Notion as the phone drop" before he knew the PWA capture already exists, so present the switch and let him pick.
- The 10-second "sign" capture: what a sign is (a quote, a screenshot, a sentence, a link) and how he logs one from his phone without thinking.
- Image sourcing: generated, saved from the web, his own photos, or all three; where the saved ones currently live.
- Read-back cadence: nightly, weekly, or on demand; and the channel (OS notification, Telegram, the Claude tab).
- Nothing on naming; the four words are locked above.

## Phase 1: research before building

Ground the frame; do not assert it from memory. Deliver as a short section inside Entry 004, not a separate doc.
- Devotion versus discipline: self-determination theory (intrinsic vs controlled motivation), bhakti/devotional practice framings, the current "devotion not discipline" line and who is saying it. Say which parts hold up and which are Instagram.
- Personal mythology as a practice: Jung's personal myth and the Red Book, Feinstein and Krippner's *Personal Mythology* (the actual method), McAdams on narrative identity, Campbell and the critiques. Give the mechanism PG can repeat to someone, not a reading list.
- Archives of self that survive neglect: commonplace books, Warburg's Mnemosyne Atlas, Are.na, artists' reference walls. The structure must resume cleanly after months away, because that is his failure mode.

## Phase 2: build, all in this repo

1. **Pages pipeline.** New capture destination `pages` (per Phase 0 answer). API route to save, list, and export. Nightly export script to `~/cortex/knowledge/journal/pages/`. The 2026-09-01 file is entry one; import it.
2. **Canon view.** A new tab in the OS on the Laputa variants. Structure: the Myth at the top (canonical text, the one thing that gets replaced not appended), then the six regions in PG's words (Life, Brain, Genius, Expression, Dreams, Aesthetic), each holding its slice of the myth plus its signs, quotes, and lore images. Signs are dated one-liners with phone-fast capture. Lore is the wall: images and quotes, masonry, each with source, the region it belongs to, and PG's one-line note. Chapters are dated entries when the myth changes, append-only. Migration `008_canon.sql` for myth versions, signs, lore items, chapters. Markdown mirror to `~/cortex/self/CANON.md` and image files to `~/cortex/self/wall/` on every write so nothing is trapped in Supabase. The wall already has five images and a manifest at `~/cortex/self/wall/`; import them as the first lore items (note column is blank; ask PG for the notes in the grill).
3. **Entry 004: The Devotion Turn.** The seed of the Myth. Written in the same register as 001-003 and added to `self/center-of-self.html`. Contents: what PG brought (devotion, his sentence), what the archive already said, where they agree and disagree, the research verdict, and the first draft of the myth: who he is and who he is becoming, written as myth (figure, origin, trials, current chapter), every beat anchored to his own words with a source pointer (archive doc or pages file). Use the 2026-09-01 pages heavily: the sediment and the clear water, the last firework, the kid from Holes, the dog and the price of loving something. End with what the myth does not yet answer. This draft also seeds the Myth at the top of the Canon view. It ships as DRAFT; nothing becomes canonical without his "that one."
4. **The witness agent.** A scheduled run (agent-runner.sh or a Claude Code cloud routine, whichever is already live for morning-briefing) at the cadence he picks: reads new pages since last run, proposes chapter drafts and myth edits, posts them to the Canon view as pending, tells him in plain words what changed and why. He approves in the OS. Devotion frame in the agent's voice: it reports what he showed up for, never what he missed. No shame strings, no streaks, no red.
5. **Images.** 8 to 12 first images from prompts derived from the myth on the Emaki x Laputa image DNA (`~/.claude/rules/visual-style.md`). Prompts stored with each lore item so they are reproducible. Tell him cost per image before generating; do not spend without a yes.
6. **Deploy** to the existing Vercel project (pg-os.vercel.app). Give the URL of the new tab. Before the Canon tab goes in, triage the dead tiles on Home: Calendar, Vitals, Now Playing all show "Connect" (OAuth gone), Projects is empty on Vercel (filesystem-only). For each, put one option to PG: reconnect now, or hide it until reconnected. The front door must not open onto three Connect buttons.
7. **LEDGER.** One line in `~/cortex/self/LEDGER.md` when it is live. Never edit prior lines.

## Phase 3: the archive gap

`~/cortex/claude-web-history/raw/` and `db/` do not exist on this Mac (gitignored, never synced). The distilled docs stop at 2026-07-13. Build from them now. Then hand PG the refresh: where in claude.ai to request the export, where to drop the zip, and the order to run `scripts/unzip.mjs`, `ingest.mjs`, `embed.mjs`, one command each, so Entry 005 can run against July 13 to today.

## Gates and rules

- Myth text and brand copy never become canonical without an explicit "that one." Drafts first.
- Ask before spend, new domains, or anything external-facing.
- No em dashes anywhere.
- Options batched with a recommendation; never a silent pick, never a bare menu.
- Build it in this session. No later phases.
- Comprehension over capability: if PG cannot explain the Canon tab in two sentences, cut until he can.
- Name machines explicitly: this is "mobile" (`patricksmith2x`); "desk" (`pg`) gets it via git.
